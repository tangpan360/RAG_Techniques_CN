# Environment notes (not parsed by pip):
# - conda env: tp-rag
# - Python: 3.12.6

# Core LangChain stack (pinned to currently installed versions)
langchain==1.3.9
langchain-core==1.4.7
langchain-openai==1.3.2
langchain-text-splitters==1.1.2
langchain-anthropic==1.4.6
langchain-community==0.4.2
langchain-experimental==0.4.2

# OpenAI + tokenization (pinned to currently installed versions)
openai==2.41.1
tiktoken==0.13.0

# Common utilities
python-dotenv==1.2.2
pydantic==2.13.4
requests==2.34.2
numpy==2.4.6
pandas==2.3.3
scikit-learn==1.7.2

# Simple RAG notebook dependencies (pinned to currently installed versions)
pypdf==6.13.2
faiss-cpu==1.14.3
PyMuPDF==1.27.2.3
rank-bm25==0.2.2
deepeval==4.0.6

# Multi-modal RAG (ColPali) notebook dependencies (pinned to currently installed versions)
byaldi==0.0.7
pillow==11.3.0
transformers==5.12.1

# JSON RAG notebook dependencies (pinned to currently installed versions)
sentence-transformers==5.6.0
jrag==0.0.1

# HyPE notebook dependencies (pinned to currently installed versions)
tqdm==4.68.2

# Notebook UX (optional, pinned to currently installed versions)
ipywidgets==8.1.8
jupyterlab-widgets==3.0.16

matplotlib==3.11.0

grouse==0.4.2
graphrag==3.1.0

ddgs==9.14.4